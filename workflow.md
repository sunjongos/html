---
name: /html
description: 즉석 1회용 커스텀 웹 도구(HTML) 생성 워크플로우. 프롬프트 기반으로 대시보드, 툴, 프로토타입 등을 3단 분리하여 최고급 UI로 자동 생성합니다.
---

# 🌐 /html 명령어 (HTML Custom Tool Generator)

## 📌 개요 (Overview)
`/html` 명령어는 Luca(Antigravity) 요원이 사용자(대표님)의 요구사항에 맞춰 **즉석에서 1회용 커스텀 웹 도구 및 UI를 생성**하는 마법의 스킬입니다. 
복잡한 설정이나 코딩 지식 없이, 직관적인 프롬프트만으로 대시보드, 코드 리뷰 시각화, 프로토타입 파라미터 튜너, 데이터 분류 도구 등을 즉각적으로 렌더링합니다. 생성된 도구를 브라우저에서 직접 조작하고, 결과값을 다시 AI에게 전달(Loop)하는 완벽한 워크플로우를 제공합니다.

## 🚀 명령어 구문 (Syntax)
`/html [옵션] "요구사항 프롬프트"`

### ⚙️ 주요 옵션 (Options)
- `--type [dashboard | tool | prototype | report]` : 생성할 웹 도구의 성격을 규정하여 최적화된 프레임워크와 UI를 적용합니다.
- `--theme [light | dark | medical | ndb]` : 시각적 테마를 설정합니다. (`medical` 또는 `ndb` 선택 시 남양주백병원의 신뢰감 있는 블루/화이트 톤 및 CI 컬러 차용)
- `--export [local | replit]` : 생성된 파일들의 저장 경로 및 배포 방식을 지정합니다.
- `--split` : 코드를 `index.html` (뼈대), `style.css` (스타일), `script.js` (인터랙션/로직) 3단으로 엄격히 분리하여 생성합니다. (기본값은 단일 HTML 통합)

---

## 📢 카파시 통신 프로토콜 (The Karpathy Communication Protocol)
모든 에이전트는 정보 교환 시 다음의 이원화된 원칙을 절대적으로 준수합니다.
1. **컴퓨터(Agent) 간의 통신**: **마크다운(`.md`)** 포맷을 기본으로 합니다. (토큰 최적화 및 초고속 파싱 목적)
2. **인간(User)과의 통신 및 협업**: **HTML** 생성을 기본 원칙으로 합니다. 대표님의 요청, 의견 조율, 혹은 외부 인원(타 부서, 고객 등)과의 공유가 필요한 모든 순간에는 텍스트가 아닌 **'조작 가능한 HTML UI/UX'**로 응답하여 인간의 인지 부하를 최소화합니다.

---

## 🧠 3단계 실행 파이프라인 (Action Flow)

에이전트는 `/html` 명령어 호출 시 다음 3단계를 자동으로 수행하여 완벽한 결과물을 도출합니다.

### Phase 1. UI/UX 요구사항 분석 및 구조화 (Parse)
1. **의도 및 기능 추출**: 대표님의 프롬프트에서 핵심 상호작용(Interactive) 요소(슬라이더, 드래그 앤 드롭, 버튼, 폼 등)를 완벽히 파악합니다.
2. **I/O 아키텍처 정의**: 입력값(파라미터 조절)과 출력값(그래프 변화, 최종 결과물 텍스트 복사 등) 및 데이터 구조를 사전에 기획합니다.

### Phase 2. 코드 생성 및 분리 (Generate & Split)
모바일 퍼스트(Mobile-first)와 모던 UI(Grid, Flexbox, Glassmorphism, Micro-animations) 원칙을 적용하여 코드를 렌더링합니다.
- **index.html**: 시맨틱 태그 기반의 깔끔한 구조. 외부 CSS/JS 링크가 포함된 완벽한 DOM 트리.
- **style.css**: 최신 트렌드를 반영한 미려하고 전문적인(Premium & Rich Aesthetics) 디자인 시스템 적용.
- **script.js**: 데이터 연산, DOM 조작, 상태 관리, 'Copy as Markdown/JSON' 등 클립보드 복사 로직 등 핵심 기능 구현.

### Phase 3. 워크스페이스 배포 및 루프(Loop) 준비 (Deploy)
1. 지정된 로컬 워크스페이스에 파일들을 자동 저장합니다.
2. 즉시 브라우저에서 렌더링하여 확인할 수 있는 절대 경로를 대표님께 제공합니다.
3. **역결합(Loop) 유도**: 조작 후 도출된 결과값을 복사하여 다시 에이전트에게 전달할 수 있도록 안내합니다.

---

## 💡 5대 핵심 활용 시나리오 (Use Cases)

프롬프트의 목적에 따라 에이전트는 다음 5가지 특화된 템플릿 패턴을 차용합니다.

### 1. 기획안/디자인 옵션 비교 대시보드 (`--type dashboard`)
텍스트로 나열된 장단점 대신, 한눈에 비교할 수 있는 시각적 카드 UI와 그리드(Grid) 대시보드를 생성합니다.
- **특징**: 여러 옵션의 트레이드오프(Trade-off) 라벨링, 직관적인 상태 비교.

### 2. 코드 리뷰 및 PR 시각화 문서 (`--type report`)
GitHub의 답답한 화면 대신, 색상과 주석이 들어간 인터랙티브 리뷰 문서를 생성합니다.
- **특징**: 위험도(Risk)별 배경 색상 맵핑, 코드 라인별 Diff 시각화 및 토글형 상세 주석.

### 3. UI 프로토타입 및 수치 조율 도구 (`--type prototype`)
애니메이션 속도, 색상, 레이아웃 크기 등을 실시간으로 조절하고 값을 추출하는 튜너(Tuner)를 만듭니다.
- **특징**: 실시간 Preview 연동. 최적의 파라미터를 찾아 AI에게 다시 전달할 수 있는 **'복사하기(Copy)' 버튼** 필수 구현.

### 4. 시각 자료가 포함된 고품질 리포트 (`--type report`)
신입 사원 교육, 인수인계, 아키텍처 설명 등에 사용할 수 있는 완성형 문서입니다.
- **특징**: 코드 베이스나 비즈니스 로직을 나타내는 SVG 다이어그램 포함, 시각적 흐름 가이드.

### 5. 1회용 커스텀 데이터 에디터 (`--type tool`) 🌟 최고 마법 스킬
엑셀이나 외부 툴에서 하던 '노가다' 작업을 완벽히 대체할 드래그 앤 드롭 웹페이지를 즉석에서 만듭니다.
- **특징**: 데이터(예: 티켓, 아이템)를 마우스로 직접 드래그하여 칸반(Kanban) 보드 등으로 분류.
- **핵심**: 조작이 끝난 후, 버튼 클릭 한 번으로 결과가 마크다운(Markdown)이나 CSV로 클립보드에 복사되는 **역결합(Loop)** 인터페이스 필수 적용.

---

## 🎯 실행 예시 (Examples)

- **데이터 분류 도구**: 
  `/html --type tool "여기 리니어(Linear) 티켓 30개가 있어. 이 티켓들을 'Now, Next, Later, Cut' 4개의 칸으로 마우스 드래그해서 옮길 수 있는 페이지로 만들어 줘. 다 정리한 후 마크다운으로 복사하는 버튼도 하단에 만들어 줘."`
- **프로토타입 튜너**: 
  `/html --type prototype --split "체크아웃 버튼의 클릭 애니메이션을 테스트할 수 있는 HTML을 만들어 줘. 재생 속도와 두근거림 정도를 슬라이더로 조절하게 하고, 마음에 드는 파라미터를 복사할 수 있는 버튼을 넣어 줘."`
- **의료용 대시보드**: 
  `/html --type dashboard --theme medical "EEN과 PEN 수술 예후를 한눈에 비교하는 인터랙티브 슬라이더 도구를 만들어줘."`

---

## ⚠️ 에이전트 준수 사항 (Constraints)
1. **Premium Design**: 디자인은 무조건 '최고급(WOW-factor)'을 지향해야 합니다. 브라우저 디폴트 스타일이나 조잡한 UI는 절대 허용되지 않습니다.
2. **Actionable Output**: HTML 생성 후 대표님께 브라우저에서 여는 방법과, 결과값을 다시 붙여넣어(Loop) 다음 업무로 이어가는 방법을 명확히 안내하십시오.
3. **Interactive & Dynamic**: 마이크로 애니메이션, 호버 효과, 부드러운 트랜지션 등을 적용하여 살아있는(Alive) 도구를 만드십시오.
4. **Mobile Responsive & Accessibility (추가/개선)**: 모바일 환경(Telegram 등)에서 대표님이 바로 열어보셔도 완벽히 조작 가능하도록 반응형 디자인(Media Queries)을 필수로 적용하고, 접근성(a11y) 기준을 준수하십시오.
5. **Modern CDN Integration (추가/개선)**: 복잡한 데이터 시각화나 상태 관리가 필요할 경우 Chart.js, Tailwind CSS, Alpine.js 등을 CDN으로 즉각적으로 끌어와 기능의 한계를 돌파하십시오.
